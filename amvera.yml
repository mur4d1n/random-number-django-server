meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  command: apt-get update; apt-get install -y redis mkcert; service redis-server start; mkcert -install; mkcert -cert-file cert.pem -key-file key.pem server-mur4d1n.amvera.io; python3 manage.py makemigrations; python3 manage.py migrate; python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:8000
  containerPort: 8000
